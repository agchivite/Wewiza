from setuptools import setup, find_packages

setup(name="market-03", version="1.0", packages=find_packages())

"""
# Para ejecutar la búsqueda de paquetes en todo el proyet (estando en el directorio donde se encuentre setup.py)
pip install -e .
"""
